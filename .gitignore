# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
htmlcov/
.coverage

# Environment & secrets
.env
*.env
confluence-api-key.txt
aws-creds.txt

# Terraform
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/terraform.tfvars
terraform/generated-key.pem

# SQLite DB (lives in rita_output — not source)
*.db
*.sqlite

# Large data files (imported to DB, not shipped in git)
data/input/NIFTY/nifty_option_bhav.csv

# Test results
test-results/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Runtime logs (never commit — locked by server, runtime data only)
logs/*.jsonl
logs/*.log
logs/*.csv
