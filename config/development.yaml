server:
  reload: true
  log_level: "debug"

model:
  path: "models"

optimizer:
  sharpe_floor: 0.5
  mdd_ceiling: 20.0

security:
  cors_origins: ["http://localhost:3000", "http://localhost:8000", "null"]


